# Core dependencies for Dealer Scraper MVP
# Note: Playwright is accessed via MCP tools (local mode) or RunPod API (cloud mode)

# HTTP client and environment variable management
# Required for RunPod serverless API integration
requests>=2.31.0
python-dotenv>=1.0.0
